INICIO%PROGRAMA{
    int A;
    int B;
    float C;
    float D;

    float F4 (cv int W, float Z){
        int X;
        X = 1I;
        Z = W - X;
    };

    int F3 (int X, cv float J){
        int Z;
        int W;
        W = Z + 1I;
        return (W);
    };

    C = F4(B -> W, D -> Z);
    C = F4(D -> Z, B -> W);

    A = F3(B -> X, B -> J);
    A = F3(B -> J, B -> X);

    A = F3(C -> X, C -> J);
    
    C = F3(B -> X, B -> J);
    A = F4(B -> W, D -> Z);
}
